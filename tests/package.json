{
  "name": "heartvoice-monitor-tests",
  "version": "1.0.0",
  "description": "TestSprite generated test suite for HeartVoice Monitor platform",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --ci --coverage --watchAll=false",
    "test:api": "jest api",
    "test:integration": "jest integration",
    "test:performance": "jest performance",
    "test:security": "jest security",
    "test:report": "jest --coverage && open ../coverage/lcov-report/index.html",
    "test:verbose": "JEST_VERBOSE=true jest --verbose"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.8",
    "@types/node": "^20.9.0",
    "jest": "^29.7.0",
    "jest-environment-node": "^29.7.0",
    "jest-html-reporters": "^3.1.5",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  }
}
